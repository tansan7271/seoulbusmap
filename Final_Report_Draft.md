# SeoulBusMap 분석 단계 보고서

## [표지 정보]

- **과목명:** 디지털아카이빙과 데이터시각화
- **프로젝트명:** SeoulBusMap: 서울시 대중교통 소외 지역 분석 및 시각화 아카이브
- **팀원:** 김도균, 김도연, 유형준, 이지민
- **제출일:** 2025. 11. 30.

---

## 1. 데이터 전처리 (Data Preprocessing)

### 1.1 수집 데이터의 성격과 전처리의 필요성

본 프로젝트에서 수집한 초기 데이터는 서울시 열린데이터광장과 카카오맵 API에서 가져온 '원시 데이터(Raw Data)'였습니다. 이 데이터들은 다음과 같은 특징을 가지고 있어, 그대로 분석에 활용하기에는 한계가 있었습니다.

1.  **파편화된 정보:** 버스 승하차 정보(`BusData`)는 '정류장' 단위로 기록되어 있어, 우리가 분석하고자 하는 '행정동' 단위의 인구 데이터(`HangJeongDong`)와 직접적인 결합이 불가능했습니다.
2.  **시계열적 노이즈:** 일자별 승하차 데이터는 날씨, 공휴일 여부, 요일 등에 따라 변동성이 매우 큽니다. 특정 하루의 데이터만으로는 해당 지역의 일반적인 교통 패턴을 대변하기 어렵다고 판단했습니다.
3.  **데이터의 결결:** 행정구역 개편이나 API 상의 누락으로 인해, 실제로는 사람이 살고 있음에도 정류장 정보가 '0'으로 잡히는 이상치(Outlier)가 존재했습니다.

따라서 우리는 이러한 원시 데이터를 **'분석 가능한 지표(Analytical Indicators)'**로 변환하기 위해 다단계의 전처리 과정을 설계하고 수행했습니다.

### 1.2 전처리 진행 경과 상세

전처리는 크게 **공간적 결합**, **시간적 요약**, **이상치 정제**의 세 단계로 진행되었습니다.

1.  **공간적 결합 (Spatial Aggregation):**

    - 먼저 카카오맵 API를 통해 수집한 `BusStop` 테이블의 위경도 좌표를 기반으로, 각 정류장이 어느 행정동에 속하는지(`district_id`)를 매핑했습니다.
    - 이후, 수백만 건에 달하는 `BusData`(승하차 로그)를 정류장 ID를 기준으로 그룹화(Group By)한 뒤, 이를 다시 행정동 단위로 재집계(Re-aggregation)했습니다. 이 과정을 통해 '정류장별 이용객' 데이터가 **'행정동별 총 교통량'** 데이터로 변환되었습니다.

2.  **Hot/Cold 데이터의 통합 (Data Integration):**

    - 본 프로젝트의 아카이빙 시스템은 최신 데이터(`BusData`)와 과거 데이터(`BusDataHistory`)를 분리하여 저장하는 **Hot/Cold 아키텍처**를 채택하고 있습니다.
    - 하지만 통계 분석에 있어서는 특정 시점의 데이터만으로는 대표성을 갖기 어렵습니다. 따라서 분석 단계에서는 이 두 테이블의 데이터를 **물리적으로 병합(Merge)**하여, 수집된 **모든 기간의 이력 데이터**를 분석 모집단으로 활용했습니다.
    - 이를 통해 최근의 트렌드(Hot)와 과거의 누적 패턴(Cold)을 모두 포괄하는, 가장 신뢰도 높은 '일평균 이용객 수'를 산출할 수 있었습니다.

3.  **시간적 요약 및 파생 변수 생성 (Temporal Summarization):**

    - 단순히 한 달 치 총 이용객 수를 더하는 것은 '평일'과 '주말'의 서로 다른 이동 패턴을 뭉뚱그릴 위험이 있었습니다.
    - 우리는 `timestamp` 필드에서 요일 정보를 추출하여 데이터를 **'주중(Weekday)'**과 **'주말(Weekend)'** 그룹으로 분리했습니다.
    - 각 그룹별로 **'일평균 이용객 수'**를 산출함으로써, 수집 기간이 서로 다른 데이터 간에도 공정한 비교가 가능하도록 정규화(Normalization)했습니다.
    - 나아가, 주중 대비 주말의 이용 비율을 나타내는 **'주말 집중도 지수(Weekend Intensity Index)'**라는 새로운 파생 변수를 생성하여, 각 행정동의 기능적 특성을 수치화했습니다.

4.  **이상치 식별 및 제외 (Outlier Handling):**

    - 분석 초기, 회귀 분석 결과에서 '상일동', '용신동' 등이 극단적인 이상치로 나타나는 것을 발견했습니다. 확인 결과, 이들은 인구는 존재하나 정류장 데이터가 0개로 수집된 케이스였습니다.
    - 이를 그대로 포함할 경우 전체 통계 모델의 신뢰도를 심각하게 훼손할 수 있다고 판단하여, **인구가 0명이거나 정류장이 0개인 3개 행정동(상일동, 용신동, 일원2동)을 '분석 제외(Excluded)' 그룹으로 분리**했습니다. 결과적으로 유효한 422개 행정동만을 분석 모집단으로 확정했습니다.

5.  **결측 일자 보정 (Missing Date Handling):**
    - 수집 과정에서 서버 점검이나 외부 API 오류로 인해 특정 일자의 데이터가 누락될 수 있습니다. 단순히 총합(Sum)을 사용할 경우 데이터가 누락된 지역의 이용량이 과소평가될 위험이 있습니다.
    - 따라서 본 프로젝트에서는 **'총 이용객 수'가 아닌 '일평균(Daily Mean) 이용객 수'를 지표로 채택**하여, 데이터가 존재하는 일자들만의 평균을 구함으로써 결측치의 영향을 최소화했습니다.

### 1.3 데이터 성질의 변화에 대한 고찰

전처리 과정을 거치며 데이터의 성질은 다음과 같이 변화했습니다.

- **물리적 로그(Log) $\rightarrow$ 의미적 지표(Insight):** 초기 데이터가 단순히 "몇 시에 몇 명이 탔다"는 물리적 사실의 기록이었다면, 전처리된 데이터는 "이 지역은 주말에 사람이 몰린다", "이 지역은 인구 대비 정류장이 부족하다"는 의미를 내포한 지표로 진화했습니다.
- **정적인 통계 $\rightarrow$ 관계적 통계:** 개별적으로 존재하던 인구 테이블과 버스 테이블이 결합됨으로써, **'수요(인구) 대비 공급(정류장)'**이라는 관계적 맥락을 파악할 수 있게 되었습니다.

### 1.4 최종 데이터 정리

- **분석 대상:** 서울시 422개 행정동 (전체 425개 중 유효 데이터)
- **데이터 구조 (DataFrame):**
  - `district_id`: 행정동 코드 (Key)
  - `population`: 생활 인구 수 (수요 변수)
  - `busstop_count`: 정류장 수 (공급 변수)
  - `avg_weekday`: 주중 일평균 승하차 인원
  - `avg_weekend`: 주말 일평균 승하차 인원
  - `weekend_intensity_index`: 주말 집중도 지수 (파생 변수)

---

## 2. 탐색적 분석 및 방법론 (Exploratory Analysis & Methodology)

## 2. 탐색적 분석 및 방법론 (Exploratory Analysis & Methodology)

### 2.1 데이터의 분포와 패턴 발견

본격적인 통계 분석에 앞서, 서울시 대중교통의 전반적인 현황을 파악하기 위해 다양한 기술 통계량을 산출하고 패턴을 탐색했습니다.

#### **1) 기초 통계량 탐색**

- **인구 대비 정류장 수:** 서울시 행정동 평균 인구 1만 명당 정류장 수는 약 **11.99개**로 나타났습니다. 하지만 최대 38.8개에서 최소 2.5개까지 지역 간 편차가 매우 컸습니다.
- **상관관계 탐색:**
  - 인구수 vs 정류장 수: $r=0.60$ (양의 상관관계)
  - 인구수 vs 주중 이용객: $r=0.72$ (강한 양의 상관관계)
  - 정류장 수 vs 주중 이용객: $r=0.62$
  - _해석:_ 정류장 수보다는 **인구수가 실제 버스 이용량과 더 강한 상관관계**를 보였습니다. 이는 "사람이 많은 곳에 버스도 많이 탄다"는 상식을 확인시켜 줍니다.

#### **2) '주말 집중도 지수'의 발견**

여러 지표를 검토하던 중, 가장 흥미로운 패턴은 **'주말 집중도 지수'의 분포**였습니다.

- **평균(Mean): 0.71**
- **중위수(Median): 0.70**

이 수치가 시사하는 바는 명확합니다. **서울시 전체적으로 볼 때, 주말의 버스 이용객은 평일의 약 70% 수준으로 감소한다**는 것입니다. 이는 통근 및 통학 수요가 대거 빠져나가는 주말의 일반적인 도시 패턴을 반영합니다.

이러한 '0.71'이라는 기준점은 이후 통계 분석에서 '문화/상업 지구'를 분류하는 결정적인 기준(Baseline)이 되었습니다. 즉, 주말 이용객이 평일과 비슷하기만 해도($Index \approx 1.0$), 그곳은 서울시 평균($0.7$)을 훨씬 상회하는 **매우 특수한 상업/문화적 핫플레이스**임을 의미하기 때문입니다.

### 2.2 통계적 지표의 정의 및 산출 근거

탐색적 분석 결과를 바탕으로, 본 분석에서 사용할 주요 지표와 통계적 기준을 다음과 같이 정의하였습니다.

#### **1) 주말 집중도 지수 (Weekend Intensity Index, $WII$)**

각 행정동의 요일별 유동 인구 특성을 정량화하기 위해 고안한 지표입니다.

$$ WII*{i} = \frac{\bar{U}*{weekend, i}}{\bar{U}\_{weekday, i}} $$

- $\bar{U}_{weekday, i}$: $i$ 행정동의 주중(월~금) 일평균 승하차 인원
- $\bar{U}_{weekend, i}$: $i$ 행정동의 주말(토~일) 일평균 승하차 인원
- **의미:** $1.0$이면 주중과 주말의 인구가 동일함을, $1.0$보다 크면 주말에 인구가 유입됨을 의미합니다.

#### **2) 잔차 (Residual, $e$)**

단순한 정류장 개수의 비교가 아닌, '인구 규모를 고려한' 공급의 적정성을 판단하기 위해 회귀 분석의 잔차를 사용했습니다.

$$ e_i = Y_i - \hat{Y}\_i = Y_i - (aX_i + b) $$

- $Y_i$: 실제 정류장 수 (Observed)
- $\hat{Y}_i$: 인구 비례 예측 정류장 수 (Predicted)
- $X_i$: 인구 수
- **의미:** $e_i < 0$ (음수)인 경우, 인구 규모에 비해 정류장이 통계적으로 예측되는 수준보다 부족함을 의미합니다.

#### **3) 통계적 분석 방법론 (Statistical Methods)**

본 분석의 신뢰성을 확보하기 위해 다음과 같은 검증된 통계적 기법을 적용하였습니다.

- **상관 분석 (Correlation Analysis):**
  - 두 변수(인구-정류장) 간의 선형적 관계 강도를 측정하기 위해 **피어슨 상관계수(Pearson Correlation Coefficient, $r$)**를 사용했습니다.
  - 구현: `numpy.corrcoef` 라이브러리를 활용하여 공분산을 표준편차의 곱으로 나눈 값을 산출했습니다.
- **회귀 분석 (Regression Analysis):**
  - 인구($X$)에 따른 적정 정류장 수($Y$)의 추세선을 도출하기 위해 **최소자승법(Ordinary Least Squares, OLS)**을 사용했습니다.
  - 구현: `numpy.polyfit(degree=1)`을 사용하여 오차의 제곱합을 최소화하는 기울기($a$)와 절편($b$)을 도출했습니다.
  - 이 회귀식($Y = aX + b$)이 곧 미시적 분석에서 각 행정동의 과부족을 판단하는 **기준선(Baseline)**이 됩니다.

#### **4) 임계값(Threshold) 설정의 통계적 근거**

도시 기능 분류를 위한 임계값은 자의적인 설정이 아닌, **실제 데이터의 분포(Distribution)**를 기반으로 설정했습니다.

- **데이터 분포:** 서울시 전체 행정동의 $WII$ 평균($\mu$)은 **0.71**입니다.
- **설정 논리:**
  - **업무 지구 ($WII < 0.65$):** 평균에서 하위 방향으로 유의미하게 벗어난 구간 (하위 약 25%).
  - **문화/상업 지구 ($WII > 0.85$):** 평균에서 상위 방향으로 유의미하게 벗어난 구간 (상위 약 5~10%).
  - **주거 지구 ($0.65 \le WII \le 0.85$):** 평균을 중심으로 한 표준적인 분포 구간.

---

## 3. 통계 분석 결과 (Statistical Analysis Results)

### 3.1 가설 1: "인구가 많으면 정류장도 많아야 한다" (수요-공급 균형)

- **분석 목적:** 서울시 대중교통 공급이 인구 수요에 맞춰 합리적으로 배분되어 있는지 검증하고, 그렇지 못한 **'교통 소외 지역'**을 찾아내고자 했습니다.
- **분석 방법 (Linear Regression & Residual Analysis):**
  - 단순히 정류장 개수가 적은 곳을 찾는 것은 의미가 없습니다. 인구가 적어서 정류장이 적은 것은 자연스러운 현상이기 때문입니다.
  - 따라서 우리는 **선형 회귀 분석**을 통해 '인구에 따른 적정 정류장 수'의 추세선을 구하고, 실제 정류장 수가 이 추세선보다 얼마나 부족한지를 나타내는 **'잔차(Residual)'**를 계산했습니다.
- **분석 결과 상세:**
  - **상관계수($r$): 0.58**
    - 인구와 정류장 수는 양의 상관관계를 보이지만, 0.7 미만으로 아주 강한 선형 관계는 아닙니다. 이는 인구 외에도 지하철망, 도로 구조 등 다른 요인이 정류장 배치에 영향을 미치고 있음을 시사합니다.
  - **회귀식 도출:** $Y = 0.0006X + 10.67$
    - **기울기(0.0006):** 인구 1만 명당 약 6개의 정류장이 추가로 필요하다는 경향성을 의미합니다.
    - **절편(10.67):** 인구가 0명이라도 최소한의 교통망 유지를 위해 약 10~11개의 정류장이 기본적으로 존재한다는 의미로 해석될 수 있습니다.
  - **소외 지역 도출 (잔차 분석):**
    - 잔차($e_i$)가 큰 음수라는 것은, 해당 인구 규모에서 기대되는 정류장 수보다 실제 설치된 정류장이 턱없이 부족하다는 뜻입니다.
    - 분석 결과, **북아현동(잔차 -35.1)**과 **역삼1동(잔차 -34.0)**이 가장 큰 공급 부족 지역으로 나타났습니다.
    - 특히 역삼1동은 절대적인 정류장 수는 많지만(51개), 막대한 유동인구(11만 명)를 고려했을 때 회귀 모델은 약 85개를 요구하고 있어, 상대적 박탈감이 가장 큰 지역임이 통계적으로 입증되었습니다.

### 3.2 가설 2: "버스 이용 패턴으로 동네의 성격을 알 수 있다" (도시 기능 분류)

- **분석 목적:** 단순한 인구 통계를 넘어, 요일별 유동 인구 패턴을 통해 해당 지역이 **'잠만 자는 동네(주거지)'**인지, **'놀러 가는 동네(문화지)'**인지 분류해보고자 했습니다.
- **분석 방법 (Classification with Thresholds):**
  - 앞서 정의한 **임계값(0.65, 0.85)**을 적용하여 서울시 422개 행정동을 분류했습니다.
- **분석 결과:**
  - **문화/상업 지구 (23곳):** 이 기준을 적용한 결과, **필동(1.42), 이태원1동(1.11), 장충동(1.14)** 등 서울의 대표적인 관광 및 상업 지구가 정확하게 분류되었습니다. 이는 우리가 설정한 가설과 분석 방법이 현실을 잘 반영하고 있음을 보여줍니다.
  - **업무 지구 (97곳):** 가산동, 여의동 등 주말이면 텅 비는 오피스 타운들이 이 그룹에 속했습니다.

### 3.3 통계 분석 API 제공

본 프로젝트는 이러한 분석 결과를 내부적으로만 활용하는 데 그치지 않고, 웹 서비스의 형태(`/analysis/`)로 구현하여 실시간으로 결과를 제공하고 있습니다. 서버는 매일 새로운 데이터를 수집하고, 사용자가 접속할 때마다 최신 데이터를 바탕으로 위와 같은 통계 분석을 다시 수행하여 가장 업데이트된 인사이트를 시각화하여 보여줍니다.

---

## 4. 결론 및 제언

본 프로젝트를 통해 우리는 공공데이터와 통계적 기법을 결합하여 서울시 대중교통의 사각지대를 데이터로 증명할 수 있었습니다. 특히 **'잔차 분석'을 통한 소외 지역 발굴**과 **'주말 집중도'를 통한 도시 기능 분류**는 기존의 단순 통계보다 훨씬 심층적인 도시 이해를 가능하게 했습니다. 향후 이 분석 모델에 지하철 접근성 데이터까지 결합된다면, 더욱 정교한 교통 정책 수립의 근거 자료로 활용될 수 있을 것입니다.
